import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mitre-10-trade',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
